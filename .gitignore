# Dependencies
node_modules/
.pnp/
.pnp.js

# Builds
dist/
dist-ssr/
build/
*.tsbuildinfo
*.local

# Note: the generated contract (apps/api/openapi.json) and client
# (packages/api-client/src/generated/) are intentionally COMMITTED — they are
# the reviewable contract artifacts and CI fails on drift. They live in
# .prettierignore (not here) so the drift check stays byte-deterministic.

# Env
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Prisma
apps/api/prisma/*.db

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
.cursor/

# Test / coverage
coverage/

# Design handoff (local reference only — see CLAUDE.md; not version-controlled)
design_handoff_command_center/
